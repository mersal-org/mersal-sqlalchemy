[project]
name = "mersal_sqlalchemy"
dynamic = ["version"]
description = "SQLAlchemy support for Mersal"
authors = [
    { name = "Abdulhaq Emhemmed", email = "abdulhaq.emhemmed@gmail.com" }
]
dependencies = [
    "mersal",
    "sqlalchemy",
    "mersal-polling",
]
readme = "README.rst"
requires-python = ">= 3.11"
license = "MIT"
license-files = ["LICENSE"]
keywords = ["service bus", "message bus", "messaging", "sqlalchemy", "sql"]

classifiers = [
  "Development Status :: 4 - Beta",
  "Natural Language :: English",
  "Operating System :: OS Independent",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: 3.14",
  "Programming Language :: Python",
  "Topic :: Software Development :: Libraries",
  "Topic :: Software Development",
  "Typing :: Typed",
  "Intended Audience :: Developers",
]

[project.urls]
Homepage = "https://github.com/mersal-org/mersal-sqlalchemy"
Documentation = "https://docs.mersal.dev"
Repository = "https://github.com/mersal-org/mersal-sqlalchemy"
Changelog = "https://github.com/mersal-org/mersal-sqlalchemy/releases"
"Issue Tracker" = "https://github.com/mersal-org/mersal-sqlalchemy/issues"

[dependency-groups]
dev = [
  { include-group = "docs" },
  { include-group = "linting" },
  { include-group = "test" },
  "sqlalchemy",
  "asyncpg",
]
docs = [
    "furo",
    "sphinx",
    "sphinx-autobuild",
    "sphinx-copybutton",
    "sphinx-design",
    "sphinxcontrib-bibtex",
    'mersal',
]
linting = [
    "basedpyright",
    "mypy",
    "pre-commit",
    "ruff",
    "ty",
]
test = [
    "pytest",
    "pytest-cov",
    "anyio",
    "mersal-msgspec",
]

[tool.uv]
managed = true

[tool.pytest.ini_options]
addopts = "--import-mode=importlib"
filterwarnings = [
    "ignore::DeprecationWarning",
]
log_cli = true

[tool.ruff]
include = [
  "src/mersal/sqlalchemy/**/*.{py,pyi}",
  "tests/**/*.{py,pyi}",
  "docs/examples/**/*.{py,pyi}",
  "pyproject.toml"
]
target-version = "py310"
line-length = 120

src = [
  "src",
]

[tool.ruff.format]
docstring-code-format = true
docstring-code-line-length = 88

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.ruff.lint.per-file-ignores]
"**/testing/**/*.py" = [
  "S101", # flake8-bandit - use of assert detected
]
"**/tests/**/*.py" = [
  "B017", # flake8-bugbear - assert-raises-exception
  "N818", # pep8-naming - error-suffix-on-exception-name
  "S101", # flake8-bandit - use of assert detected
]
"**/*/conftest.py" = [
  "F403", # Pyflakes - undefined-local-with-import-star
]

[tool.ruff.lint.isort]
known-first-party = [
"mersal",
]

[build-system]
requires = ["hatchling", "hatch-vcs"]
build-backend = "hatchling.build"

[tool.hatch.version]
source = "vcs"
fallback-version = "0.0.0"

[tool.hatch.version.raw-options]
local_scheme = "no-local-version"

[tool.hatch.build.targets.wheel]
packages = ["src/mersal"]
dev-mode-dirs = ["src"]

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.sdist]
dev-mode-dirs = ["."]
exclude = ["/.github", "/docs"]

# Type checking configuration
[tool.mypy]
python_version = "3.11"
enable_error_code = [
  "truthy-bool",
  "truthy-iterable",
  "unused-awaitable",
  "ignore-without-code",
  "possibly-undefined",
  "redundant-self",
]
packages = [
  "mersal.sqlalchemy",
]
disallow_any_generics = false
local_partial_types = true
show_error_codes = true
strict = true
warn_unreachable = true

[tool.basedpyright]
typeCheckingMode = "standard"
pythonVersion = "3.11"
include = [
  "src/mersal",
  "tests",
]

[tool.ty.environment]
python-version = "3.11"

[tool.ty.src]
include = [
  "src/mersal",
]
